/target
**/*.rs.bk
keys/
*.private-key
.DS_Store

# Node, for the plugin and the example
node_modules/
# package-lock.json is deliberately NOT ignored: a machine that clones this repo
# runs `npm ci`, which refuses to work without one.

# What a distributed run leaves behind
blob-report/
cm-blob-report/
cm-artifacts/
playwright-report/
test-results/
cm.generated.json
